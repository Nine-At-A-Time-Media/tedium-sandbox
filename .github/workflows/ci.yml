name: ci

on:
  push:
    branches:
      - main
      - tedium/merge
      - tedium/try
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: bar is green iff status.txt says green
        run: |
          grep -qx 'green' status.txt
